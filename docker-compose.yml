services:
  postgres:
    image: postgres:16-alpine
    container_name: cekap_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-cekap}
      POSTGRES_USER: ${POSTGRES_USER:-cekap}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-cekap_secret}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cekap}"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  backend:
    build: ./backend
    container_name: cekap_backend
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://${POSTGRES_USER:-cekap}:${POSTGRES_PASSWORD:-cekap_secret}@postgres:5432/${POSTGRES_DB:-cekap}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: cekap_frontend
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: cekap_nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
