name = "cekap"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"

# Run `wrangler d1 create cekap-db` and paste the database_id it returns below.
[[d1_databases]]
binding = "DB"
database_name = "cekap-db"
database_id = "REPLACE_WITH_YOUR_DB_ID"

[assets]
directory = "./dist"